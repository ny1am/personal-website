{
  "name": "website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "lint": "eslint src/**/*.{ts,astro}"
  },
  "nx": {
    "implicitDependencies": ["generate-resume"],
    "targets": {
      "preview": {
        "dependsOn": ["build"]
      },
      "build": {
        "dependsOn": ["^build"]
      }
    }
  }
}
